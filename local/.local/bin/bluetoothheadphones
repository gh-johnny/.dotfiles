#!/bin/bash

# Use your device mac address
DEVICE="XX:XX:00:00:AA:BB"

if bluetoothctl info $DEVICE | grep 'Connected: yes' -q; then
	bluetoothctl disconnect "$DEVICE"
else
	bluetoothctl connect "$device"
fi
exit 0
